# 설치 가이드 

---

## 1. npm 설치


window 터미널에서 다음 명령어를 실행

`winget install OpenJS.NodeJS.LTS`


## 2. 명령어 설정


npm이 설치되었다면, repository 폴더에 들어가서 다음 명령어를 입력

`npm install`

`npm link`

`npm install -g .`


윈도우 터미널에서 lms-console 실행

`lmsconsole`
